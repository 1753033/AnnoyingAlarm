<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.annoyingalarm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rawcode.RelaxActivity"></activity>
        <activity android:name=".alarm.AlarmScreenShakeActivity" />
        <activity android:name=".alarm.AlarmScreenMathActivity" />
        <activity android:name=".alarm.AlarmScreenActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".ChallengeActivity" />
        <activity android:name=".SleepDetailActivity" />
        <activity android:name=".SleepHistoryActivity" />
        <activity android:name=".AccountInfoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".rawcode.MoreActivity" />
        <activity android:name=".NewsDetailActivity" />
        <activity android:name=".rawcode.NewsActivity" />
        <activity android:name=".rawcode.WeatherActivity" />
        <activity android:name=".alarm.AddAlarmActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TodoListActivity" />

        <service
            android:name=".alarm.AlarmService"
            android:enabled="true" />

        <receiver android:name=".alarm.AlarmManagerHelper" />
    </application>

</manifest>